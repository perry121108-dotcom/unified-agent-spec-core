# Clean — Prompts Externalized

All Builder / Tester System Prompt strings must live under the `prompts/`
directory and be loaded at runtime via readFile. No inline prompts.

```text
short snippet only
two lines
```
